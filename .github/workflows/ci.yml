name: Continuous Integration

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test-typescript:
    name: TypeScript Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v7.0.1

      - name: Setup Node.js
        id: setup-node
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: npm

      - name: Install Dependencies
        id: npm-ci
        run: npm ci

      - name: Check Format
        id: npm-format-check
        run: npm run format:check

      - name: Lint
        id: npm-lint
        run: npm run lint

      - name: Test
        id: npm-ci-test
        run: npm run ci-test

  test-action:
    name: GitHub Actions Test
    runs-on: ubuntu-latest

    services:
      wiremock:
        image: wiremock/wiremock:latest-alpine
        ports: [8080]

    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v7.0.1

      - name: Test Local Action
        id: test-action
        uses: ./
        with:
          hostname: localhost
          port: ${{ job.services.wiremock.ports['8080'] }}
          mappings: ${{ github.workspace }}/test-stubs

      - name: Print Output
        id: output
        run: |
          echo "There are ${{ steps.test-action.outputs.total }} mappings in Wiremock."
          echo ${{ steps.test-action.outputs.mappings }}
